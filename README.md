
  # Email Notification UI

  This is a code bundle for Email Notification UI. The original project is available at https://www.figma.com/design/SQoSZtF905mxJYgnSkiL61/Email-Notification-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  